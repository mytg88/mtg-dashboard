{
  "name": "mtg-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "MTG franchise dashboard (single-page app) plus unit tests for its helper functions.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.8",
    "vitest": "^2.1.8"
  }
}
